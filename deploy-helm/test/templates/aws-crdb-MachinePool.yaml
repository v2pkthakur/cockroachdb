apiVersion: hive.openshift.io/v1
kind: MachinePool
metadata:
  name: aws-crdb-workers
  namespace: aws-crdb-workers
spec:
  clusterDeploymentRef:
    name: aws-cluster-0
  name: worker
  platform:
    aws:
      rootVolume:
        iops: 2000
        size: 100
        type: io1
      type: c5d.4xlarge
  replicas: 3
